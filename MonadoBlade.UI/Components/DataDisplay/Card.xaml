<UserControl x:Class="MonadoBlade.UI.Components.DataDisplay.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,16">
        <StackPanel Margin="16">
            <TextBlock Text="Card Title" Style="{DynamicResource TextH3}" Margin="0,0,0,12"/>
            <TextBlock Text="This is a reusable card component with consistent styling, spacing, and elevation." 
                       Style="{DynamicResource TextBody}" 
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                <Button Style="{DynamicResource ButtonSecondary}" Margin="0,0,8,0">Action</Button>
                <Button Style="{DynamicResource ButtonSecondary}">Cancel</Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
