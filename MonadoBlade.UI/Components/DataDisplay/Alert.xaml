<UserControl x:Class="MonadoBlade.UI.Components.DataDisplay.Alert"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Border Margin="0,0,0,16" Padding="16" CornerRadius="4" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource WarningBrush}" BorderThickness="1">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="⚠" FontSize="20" Foreground="{DynamicResource WarningBrush}" Margin="0,0,12,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Warning" Style="{DynamicResource TextH3}" Margin="0,0,0,4"/>
                <TextBlock Text="This is a warning message for user attention." Style="{DynamicResource TextBody}" Foreground="{DynamicResource TextSecondaryBrush}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
