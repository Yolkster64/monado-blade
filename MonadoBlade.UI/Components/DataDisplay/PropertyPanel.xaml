<UserControl x:Class="MonadoBlade.UI.Components.DataDisplay.PropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Border Style="{DynamicResource CardStyle}">
        <StackPanel Margin="16">
            <TextBlock Text="Properties" Style="{DynamicResource TextH3}" Margin="0,0,0,16"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="36"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" Style="{DynamicResource TextBody}" Margin="0,8"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="[Value]" Style="{DynamicResource TextBody}" Margin="16,8"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Type:" Style="{DynamicResource TextBody}" Margin="0,8"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="[Value]" Style="{DynamicResource TextBody}" Margin="16,8"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Status:" Style="{DynamicResource TextBody}" Margin="0,8"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="[Value]" Style="{DynamicResource TextBody}" Margin="16,8"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
