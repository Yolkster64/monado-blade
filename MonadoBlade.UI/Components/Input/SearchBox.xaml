<UserControl x:Class="MonadoBlade.UI.Components.Input.SearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Border Padding="8" Background="{DynamicResource SurfaceBrush}" CornerRadius="4" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="🔍" FontSize="16" Margin="8,0"/>
            <TextBox Background="Transparent"
                     Foreground="{DynamicResource TextBrush}"
                     BorderThickness="0"
                     Text="Search..."
                     Padding="8,8"/>
        </StackPanel>
    </Border>
</UserControl>
