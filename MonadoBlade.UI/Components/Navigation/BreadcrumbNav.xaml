<UserControl x:Class="MonadoBlade.UI.Components.Navigation.BreadcrumbNav"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <StackPanel Orientation="Horizontal" Margin="16" VerticalAlignment="Center">
        <TextBlock Text="Home" Style="{DynamicResource TextBody}" Foreground="{DynamicResource PrimaryBrush}" Cursor="Hand"/>
        <TextBlock Text=" / " Style="{DynamicResource TextBody}" Margin="8,0"/>
        <TextBlock Text="Components" Style="{DynamicResource TextBody}" Foreground="{DynamicResource PrimaryBrush}" Cursor="Hand"/>
        <TextBlock Text=" / " Style="{DynamicResource TextBody}" Margin="8,0"/>
        <TextBlock Text="Navigation" Style="{DynamicResource TextBody}" Foreground="{DynamicResource TextSecondaryBrush}"/>
    </StackPanel>
</UserControl>
