# Monado Blade v3.4.0 - Dark Theme XAML
# File: src/MonadoBlade.GUI/Themes/MonadoDark.xaml

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- DARK THEME COLOR PALETTE -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="#93c5fd"/>
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#bfdbfe"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="#0f172a"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#1e293b"/>
    <SolidColorBrush x:Key="TextBrush" Color="#f1f5f9"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#cbd5e1"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#334155"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="#86efac"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#fcd34d"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#fca5a5"/>
    <SolidColorBrush x:Key="InfoBrush" Color="#93c5fd"/>

    <!-- BUTTON STYLE -->
    <Style x:Key="ButtonPrimaryStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="#0f172a"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="Border" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Opacity" Value="0.9"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- CARD STYLE -->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
    </Style>

</ResourceDictionary>
